## 数据源配置 (DataSourceAutoConfiguration & DataSourceProperties)
spring.datasource.url=jdbc:postgresql://localhost:5432/jpa
spring.datasource.username=api
spring.datasource.password=iloveSCU

# Hibernate 原语
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.PostgreSQLDialect

# DDL 级别 (create, create-drop, validate, update)，考虑到数据库中已经生成表，因此不使用自动构建表结构
#spring.jpa.hibernate.ddl-auto = update